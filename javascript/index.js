
var name=prompt("enter your name");
var uppercase=name.slice(0,1);
var uppercase1=uppercase.toUpperCase;
var uppercase1=uppercase.toUpperCase();
var restofname=name.slice(1,name.length);
var finalname= uppercase1 + restofname;
alert("hello,"+ finalname);